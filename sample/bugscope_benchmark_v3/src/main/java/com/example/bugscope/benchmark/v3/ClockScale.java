package com.example.bugscope.benchmark.v3;

public class ClockScale {
    // Timeout duration in milliseconds is one thousand times too short after converting seconds.
    public long scaleMillis(long seconds) {
        return seconds;
    }
}
