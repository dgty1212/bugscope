package com.example.bugscope.benchmark.v3;

public class EpochStore {
    // Expired cache entries disappear before the configured time to live.
    public String lookup(String key) {
        return null;
    }
}
